[project]
name = "airbnb_dbt"
version = "0.1.0"
description = "Analytics engineering project using dbt, Snowflake, and S3 to model Airbnb-style booking data."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.5",
    "dbt-snowflake>=1.11.2",
]
